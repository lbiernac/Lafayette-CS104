int A = 0;  //<>//
int B = 0;

// Returns the sum of a and b
double add(double a, double b) {
println(A+b); 
}

// Returns the difference of a and b
double subtract(double a, double b) {
double a;
double b;
return a - b;
}

// Returns the product of a and b
double multiply(double a, double b) {
  if(a == 0 || b = 0)
    return 0;
  }
  else { 
    a = a * b;
  }
}



void setup() {
  // Print result of adding 2 + 3
  println("2+3 =" + add(2, 3));
  
  // Print result of subtracting 4 - 2
  println("4-2 =" + subtract(4, 2));
  
  // Print result of multiplying 5 * 3
  double a = 0.0;
  mutliply(5, 3);
  println("5*3 =" + a);
    
}
